{
  // Place your snippets for javascript here. Each snippet is defined under a snippet name and has a prefix, body and
  // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
  // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the
  // same ids are connected.
  // Example:
  // "Print to console": {
  // 	"prefix": "log",
  // 	"body": [
  // 		"console.log('$1');",
  // 		"$2"
  // 	],
  // 	"description": "Log output to console"
  // }
  "console.log": {
    "prefix": "console.log",
    "body": ["console.log($TM_SELECTED_TEXT$0);"],
    "description": "log to console"
  },
  "querySelect": {
    "prefix": "qSelect",
    "body": [
      "let $TM_SELECTED_TEXT$1 = document.querySelector($TM_SELECTED_TEXT$0);"
    ],
    "description": "queryselect"
  },
  "querySelectAll": {
    "prefix": "qSelectAll",
    "body": [
      "let $TM_SELECTED_TEXT$1 = document.querySelectorAll($TM_SELECTED_TEXT$0);"
    ],
    "description": "queryselectAll"
  },
  "forEachSnippet": {
    "prefix": "forEachSnippet",
    "body": [".forEach((element, index) => {","$TM_SELECTED_TEXT$0",
    "});"
    ],
    "description": "forEach loop with index"
  },
  "addEventListenerSnippet": {
    "prefix": "addEventListener",
    "body": [
      ".addEventListener('submit', (event) => {",
      "event.preventDefault();",
      "$TM_SELECTED_TEXT$0",
      "});"
    ],
    "description": "eventListener preventDefault()"
  }
}
